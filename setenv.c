#include "main.h"
/**
 * _setenv - add or change an environment
 * @name: name of the variable
 * @value: the value of the variable
 * @overwrite: the string written on the old value
 * Return: 0 on success
 */
int _setenv(const char *name, const char *value, int overwrite)
{

}
